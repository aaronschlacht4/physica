{
  "name": "physica",
  "version": "1.0.0",
  "description": "A gravity sandbox: drag to throw a planet around a star.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aaronschlacht4/physica.git"
  },
  "author": "Aaron Schlacht",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "katex": "^0.18.6",
    "marked": "^18.0.11"
  }
}
